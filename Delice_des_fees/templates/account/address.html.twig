{% extends "base.html.twig" %}
{% block title %} {{ title }} {% endblock %}
{% block body %}
<br><br>
<div class="message"><h1> {{message}} </h1></div>

<div class="example-wrapper">
    Bienvenue {{ app.user.firstname}} {{ app.user.lastname}}. C'est ici que vous pourrez gérer toutes vos addresses.

<br>
        <a href="{{path('account')}}">Retourner à mon espace personnel <a/>
<br>
<br>
{% if app.user.addresses|length == 0 %}
<p class="text-center">

Vous n'avez pas encore enregistré d'addresses dans votre compte client. Pour en ajouter une, <a href="{{path('address_add')}}">cliquez ici</a>

</p>

{% else %}


<div class="row">
{% for address in app.user.addresses %}
<div class="col-md-4">
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{address.name}}</h5>
    <p class="card-text">
    {{address.address}} <br>
    {{address.postal}} - {{address.city}} - {{address.country}}
<hr>
    <a href="{{path('address_modif', {'id' : address.id})}}">Modifier</a> <a href="{{path('address_suppr', {'id' : address.id})}}">Supprimer</a>
  </p>
  </div>
</div>
</div>


{% endfor %}


{% endif %}
<div>
        <a href="{{path('address_add')}}" class="btn btn-success btn-sm float-right"> Ajouter une addresse </a>
</div>

</div>
{% endblock %}

